---
author: qinyi
created_at: 2026-06-03T20:35:00+08:00
updated_at: 2026-06-24T00:00:00+08:00
---

schema_version: 1
project: frontend
source_commit: ba87eec
generated_at: 2026-06-24 00:00:00
generator: sillyspec-scan
last_change: 2026-06-24 全量重扫覆盖更新；新增 admin/ppm/permissions/menu-permissions/api-keys/workspace-members/daemon/layout/charts/kanban 等模块；新增模块暂无 modules/<id>.md 文档故 needs_review:true

modules:
  # ─── App Router Pages (top-level) ──────────────────────────────────

  app-pages:
    status: active
    doc: modules/app-pages.md
    paths:
      - "frontend/src/app/page.tsx"
      - "frontend/src/app/(auth)/login/page.tsx"
      - "frontend/src/app/(dashboard)/workspaces/page.tsx"
      - "frontend/src/app/(dashboard)/runtimes/page.tsx"
      - "frontend/src/app/(dashboard)/settings/page.tsx"
      - "frontend/src/app/(dashboard)/settings/git-identities/page.tsx"
      - "frontend/src/app/(dashboard)/settings/api-keys/page.tsx"
    tags: [pages, nextjs-app-router, routes]
    aliases: [pages, 路由, 页面]
    entrypoints:
      - HomePage          # /
      - LoginPage         # /login
      - WorkspacesPage    # /workspaces
      - RuntimesPage      # /runtimes
      - SettingsPage      # /settings
      - GitIdentitiesPage # /settings/git-identities
      - ApiKeysPage       # /settings/api-keys
    main_symbols:
      - HomePage
      - LoginPage
      - WorkspacesPage
      - RuntimesPage
      - SettingsPage
      - GitIdentitiesPage
      - ApiKeysPage
    depends_on: [components-ui, components-shared, components-layout, lib-api, stores-session]
    used_by: []
    needs_review: false
    concerns: []
    review_reasons: []

  # ─── App Router Pages (admin 后台) ─────────────────────────────────

  app-admin-pages:
    status: active
    doc: modules/app-admin-pages.md
    paths:
      - "frontend/src/app/(dashboard)/admin/**/*.tsx"
    tags: [pages, nextjs-app-router, admin]
    aliases: [admin-pages]
    entrypoints:
      - AdminLayout          # /admin
      - AdminUsersPage       # /admin/users
      - AdminRolesPage       # /admin/roles
      - AdminOrganizationsPage # /admin/organizations
    main_symbols:
      - AdminLayout
      - AdminUsersPage
      - AdminRolesPage
      - AdminOrganizationsPage
    depends_on: [components-ui, components-layout, lib-admin, lib-permission, lib-menu-permissions, lib-api, stores-session]
    used_by: []
    needs_review: false
    concerns: []
    review_reasons:
      - 新增模块，modules/app-admin-pages.md 尚未生成

  # ─── App Router Pages (PPM 项目管理) ───────────────────────────────

  app-ppm-pages:
    status: active
    doc: modules/app-ppm-pages.md
    paths:
      - "frontend/src/app/(dashboard)/ppm/**/*.tsx"
    tags: [pages, nextjs-app-router, ppm, project-management]
    aliases: [ppm-pages]
    entrypoints:
      - PpmHomePage              # /ppm
      - PpmProjectsPage          # /ppm/projects
      - PpmCustomersPage         # /ppm/customers
      - PpmProjectMembersPage    # /ppm/project-members
      - PpmMilestoneDetailsPage  # /ppm/milestone-details
      - PpmTaskExecutePage       # /ppm/task-execute
      - PpmProjectStakeholdersPage # /ppm/project-stakeholders
      - PpmPlanNodesPage         # /ppm/plan-nodes
      - PpmProblemChangesForms   # /ppm/problem-changes/_forms
    main_symbols:
      - PpmHomePage
      - PpmProjectsPage
      - PpmCustomersPage
      - PpmProjectMembersPage
      - PpmMilestoneDetailsPage
      - PpmTaskExecutePage
      - PpmProjectStakeholdersPage
      - PpmPlanNodesPage
    depends_on: [components-ui, components-layout, components-charts, lib-ppm, stores-kanban, lib-api, stores-session]
    used_by: []
    needs_review: false
    concerns: []
    review_reasons:
      - 新增模块，modules/app-ppm-pages.md 尚未生成

  # ─── App Router Pages (workspace-scoped) ───────────────────────────

  app-workspace-pages:
    status: active
    doc: modules/app-workspace-pages.md
    paths:
      - "frontend/src/app/(dashboard)/workspaces/[id]/**/*.tsx"
    tags: [pages, nextjs-app-router, workspace-scoped]
    aliases: [workspace-pages]
    entrypoints:
      - WorkspaceDetailPage # /workspaces/[id]
      - ChangesPage         # /workspaces/[id]/changes
      - ChangeDetailPage    # /workspaces/[id]/changes/[cid]
      - TaskBoardPage       # /workspaces/[id]/changes/[cid]/tasks
      - TaskDetailPage      # /workspaces/[id]/changes/[cid]/tasks/[tid]
      - CreateChangePage    # /workspaces/[id]/create-change
      - AgentPage           # /workspaces/[id]/agent
      - ComponentsPage      # /workspaces/[id]/components
      - TopologyPage        # /workspaces/[id]/components/topology
      - ScanDocsPage        # /workspaces/[id]/scan-docs
      - KnowledgePage       # /workspaces/[id]/knowledge
      - RuntimePage         # /workspaces/[id]/runtime
      - ReleasesPage        # /workspaces/[id]/releases
      - ApprovalsPage       # /workspaces/[id]/approvals
      - AuditPage           # /workspaces/[id]/audit
      - IncidentsPage       # /workspaces/[id]/incidents
      - IncidentDetailPage  # /workspaces/[id]/incidents/[iid]
      - MissionsPage        # /workspaces/[id]/missions
      - MembersPage         # /workspaces/[id]/members
    main_symbols:
      - WorkspaceDetailPage
      - ChangesPage
      - ChangeDetailPage
      - TaskBoardPage
      - TaskDetailPage
      - CreateChangePage
      - AgentPage
      - ComponentsPage
      - TopologyPage
      - ScanDocsPage
      - KnowledgePage
      - RuntimePage
      - ReleasesPage
      - ApprovalsPage
      - AuditPage
      - IncidentsPage
      - IncidentDetailPage
      - MissionsPage
      - MembersPage
    depends_on: [components-ui, components-shared, components-layout, lib-api, stores-session, lib-agent-stream, lib-use-agent-run-stream, components-agent-log, lib-changes, lib-tasks, lib-workflow, lib-components, lib-scan-docs, lib-knowledge, lib-runtime, lib-releases, lib-incidents, lib-approvals, lib-audit, lib-workspace-members]
    used_by: []
    needs_review: false
    concerns:
      - 大部分 UI 内联在页面组件中，页面代码较长
    review_reasons: []

  # ─── App Layouts ───────────────────────────────────────────────────

  app-layouts:
    status: active
    doc: modules/app-layouts.md
    paths:
      - "frontend/src/app/layout.tsx"
      - "frontend/src/app/(dashboard)/layout.tsx"
    tags: [layouts, nextjs-app-router]
    aliases: [layouts]
    entrypoints:
      - RootLayout
      - DashboardLayout
    main_symbols:
      - RootLayout
      - DashboardLayout
    depends_on: [components-shared, components-layout, stores-session, lib-permission, lib-menu-permissions]
    used_by: [app-pages, app-admin-pages, app-ppm-pages, app-workspace-pages]
    needs_review: false
    concerns: []
    review_reasons: []

  # ─── API Route Handlers ────────────────────────────────────────────

  app-api-routes:
    status: active
    doc: modules/app-api-routes.md
    paths:
      - "frontend/src/app/api/**/route.ts"
    tags: [api-routes, nextjs-app-router, sse-proxy]
    aliases: [api-routes]
    entrypoints:
      - GET # /api/workspaces/[workspaceId]/agent/runs/[runId]/stream
      - GET # /api/daemon-chat/[runId]/stream
      - GET # /api/daemon/sessions/[sessionId]/stream
    main_symbols:
      - GET
    depends_on: []
    used_by: [lib-agent-stream, lib-daemon]
    needs_review: false
    concerns: []
    review_reasons: []

  # ─── Shared Components ─────────────────────────────────────────────

  components-shared:
    status: active
    doc: modules/components-shared.md
    paths:
      - "frontend/src/components/app-shell.tsx"
      - "frontend/src/components/health-card.tsx"
      - "frontend/src/components/workspace-card.tsx"
      - "frontend/src/components/workspace-scan-dialog.tsx"
      - "frontend/src/components/component-detail-drawer.tsx"
      - "frontend/src/components/sillyspec-step-progress.tsx"
      - "frontend/src/components/server-status-card.tsx"
      - "frontend/src/components/top-bar.tsx"
      - "frontend/src/components/antd-providers.tsx"
      - "frontend/src/components/error-boundary.tsx"
      - "frontend/src/components/logout-confirm-dialog.tsx"
      - "frontend/src/components/workspace-tabs.tsx"
      - "frontend/src/components/workspace-daemon-switcher.tsx"
      - "frontend/src/components/workspace-path-fields.tsx"
      - "frontend/src/components/agent-run-panel.tsx"
      - "frontend/src/components/agent-model-input.tsx"
      - "frontend/src/components/agent-provider-select.tsx"
      - "frontend/src/components/mission-console.tsx"
    tags: [components, shared-ui]
    aliases: [shared-components, 组件]
    entrypoints: []
    main_symbols:
      - AppShell
      - HealthCard
      - WorkspaceCard
      - WorkspaceScanDialog
      - ComponentDetailDrawer
      - SillySpecStepProgress
      - ServerStatusCard
      - TopBar
      - AntdProviders
      - ErrorBoundary
      - AgentRunPanel
    depends_on: [components-ui, components-layout, stores-session, lib-utils]
    used_by: [app-pages, app-admin-pages, app-ppm-pages, app-workspace-pages, app-layouts]
    needs_review: false
    concerns: []
    review_reasons: []

  # ─── UI Primitive Components ───────────────────────────────────────

  components-ui:
    status: active
    doc: modules/components-ui.md
    paths:
      - "frontend/src/components/ui/*.tsx"
    tags: [components, ui-primitives, design-system, shadcn]
    aliases: [ui-components]
    entrypoints: []
    main_symbols:
      - Button
      - Input
      - Badge
      - Avatar
      - Card
      - Dialog
      - DropdownMenu
      - EmptyState
      - Skeleton
      - StatusBadge
      - Tag
      - Tooltip
    depends_on: [lib-utils]
    used_by: [components-shared, components-agent-log, components-daemon, components-layout, app-pages, app-admin-pages, app-ppm-pages, app-workspace-pages]
    needs_review: false
    concerns: []
    review_reasons: []

  # ─── Layout Components（页面骨架/表格/表单） ──────────────────────

  components-layout:
    status: active
    doc: modules/components-layout.md
    paths:
      - "frontend/src/components/layout/*.tsx"
    tags: [components, layout, data-table, page-shell]
    aliases: [layout-components]
    entrypoints: []
    main_symbols:
      - PageContainer
      - PageHeader
      - SectionCard
      - SearchBar
      - SearchBarActions
      - FormLayout
      - DataTable
    depends_on: [components-ui, lib-utils]
    used_by: [components-shared, app-pages, app-admin-pages, app-ppm-pages, app-workspace-pages, app-layouts]
    needs_review: false
    concerns: []
    review_reasons:
      - 新增模块，modules/components-layout.md 尚未生成

  # ─── Agent Log Viewer ──────────────────────────────────────────────

  components-agent-log:
    status: active
    doc: modules/components-agent-log.md
    paths:
      - "frontend/src/components/agent-log-viewer.tsx"
      - "frontend/src/components/agent-log/*.ts"
      - "frontend/src/components/agent-log/*.tsx"
    tags: [components, agent, log-viewer]
    aliases: [agent-log]
    entrypoints: []
    main_symbols:
      - AgentLogViewer
      - normalizeLogs
      - parseToolCallContent
      - parseScanCheckOutput
      - isPendingReplied
      - isThinkingContent
      - filterToolProtocolLines
      - ToolCallPreview
      - ToolResultCard
      - CopyButton
      - CollapsibleSection
    depends_on: [components-ui]
    used_by: [app-workspace-pages]
    needs_review: false
    concerns: []
    review_reasons: []

  # ─── Daemon Components（运行时会话交互） ──────────────────────────

  components-daemon:
    status: active
    doc: modules/components-daemon.md
    paths:
      - "frontend/src/components/daemon/*.tsx"
    tags: [components, daemon, runtime-session, interactive]
    aliases: [daemon-components]
    entrypoints: []
    main_symbols:
      - RuntimeSessionDialog
      - InteractiveSessionPanel
      - InteractiveSessionChatSection
      - QuickChatSessionSection
      - SessionsSidebar
      - SessionHistoryView
      - logsToTurns
      - canResumeSession
      - isActiveSession
    depends_on: [components-ui, lib-daemon, lib-agent, stores-session]
    used_by: [app-pages, app-workspace-pages]
    needs_review: false
    concerns: []
    review_reasons:
      - 新增模块，modules/components-daemon.md 尚未生成

  # ─── Charts Components（ECharts 图表） ────────────────────────────

  components-charts:
    status: active
    doc: modules/components-charts.md
    paths:
      - "frontend/src/components/charts/*.tsx"
    tags: [components, charts, echarts, ppm]
    aliases: [charts-components]
    entrypoints: []
    main_symbols:
      - WorkHourBarChart
      - WorkHourPieChart
      - ProjectPlanCostBarChart
    depends_on: [components-ui]
    used_by: [app-ppm-pages]
    needs_review: false
    concerns: []
    review_reasons:
      - 新增模块，modules/components-charts.md 尚未生成

  # ─── Permissions Components（会话权限面板） ───────────────────────

  components-permissions:
    status: active
    doc: modules/components-permissions.md
    paths:
      - "frontend/src/components/permissions/*.tsx"
    tags: [components, permissions]
    aliases: [permissions-components]
    entrypoints: []
    main_symbols:
      - SessionPermissionPanel
    depends_on: [components-ui, lib-permission, lib-menu-permissions]
    used_by: [app-layouts]
    needs_review: false
    concerns: []
    review_reasons:
      - 新增模块，modules/components-permissions.md 尚未生成

  # ─── PPM Components（项目管理通用组件） ───────────────────────────

  components-ppm:
    status: active
    doc: modules/components-ppm.md
    paths:
      - "frontend/src/components/ppm-*.tsx"
    tags: [components, ppm, project-management]
    aliases: [ppm-components]
    entrypoints: []
    main_symbols:
      - PpmDictSelect
      - PpmFileUrls
      - PpmProjectMembersTable
      - PpmProjectPlanDetail
      - PpmProjectPlanForm
      - PpmResourceTable
      - PpmStatusActions
      - PpmSubTable
      - PpmText
      - PpmUserSelect
    depends_on: [components-ui, components-layout, lib-ppm]
    used_by: [app-ppm-pages]
    needs_review: false
    concerns: []
    review_reasons:
      - 新增模块，modules/components-ppm.md 尚未生成

  # ─── Admin Components（组织/角色/用户管理） ───────────────────────

  components-admin:
    status: active
    doc: modules/components-admin.md
    paths:
      - "frontend/src/components/admin-*.tsx"
      - "frontend/src/components/api-key-create-dialog.tsx"
      - "frontend/src/components/workspace-member-*.tsx"
      - "frontend/src/components/permission-approval-*.tsx"
      - "frontend/src/components/ask-user-dialog-card.tsx"
      - "frontend/src/components/daemon-dir-browser.tsx"
    tags: [components, admin, organization, role, api-key, workspace-member]
    aliases: [admin-components]
    entrypoints: []
    main_symbols:
      - AdminOrganizationTree
      - AdminRolePermissionPicker
      - AdminUserDrawer
      - ApiKeyCreateDialog
      - WorkspaceMemberAddDialog
      - WorkspaceMemberRow
      - PermissionApprovalCard
      - PermissionApprovalDialog
      - AskUserDialogCard
      - DaemonDirBrowser
    depends_on: [components-ui, components-layout, lib-admin, lib-menu-permissions, lib-api-keys, lib-workspace-members]
    used_by: [app-admin-pages, app-pages, app-workspace-pages]
    needs_review: false
    concerns: []
    review_reasons:
      - 新增模块，modules/components-admin.md 尚未生成

  # ─── API Client Core ───────────────────────────────────────────────

  lib-api:
    status: active
    doc: modules/lib-api.md
    paths:
      - "frontend/src/lib/api.ts"
    tags: [api, fetch, http-client, core]
    aliases: [api-client, lib, api]
    entrypoints: []
    main_symbols:
      - apiFetch
      - getApiBaseUrl
      - getDirectApiBaseUrl
      - ApiError
    depends_on: [stores-session]
    used_by:
      - lib-auth
      - lib-workspaces
      - lib-changes
      - lib-agent
      - lib-components
      - lib-scan-docs
      - lib-knowledge
      - lib-tasks
      - lib-workflow
      - lib-releases
      - lib-incidents
      - lib-approvals
      - lib-audit
      - lib-health
      - lib-settings
      - lib-git-identities
      - lib-git-gateway
      - lib-tool-gateway
      - lib-worktree
      - lib-runtime
      - lib-spec-workspaces
      - lib-archive
      - lib-change-writer
      - lib-daemon
      - lib-admin
      - lib-api-keys
      - lib-workspace-members
      - lib-ppm
      - app-pages
      - app-admin-pages
      - app-ppm-pages
      - app-workspace-pages
    needs_review: false
    concerns: []
    review_reasons: []

  # ─── Auth Client ───────────────────────────────────────────────────

  lib-auth:
    status: active
    doc: modules/lib-auth.md
    paths:
      - "frontend/src/lib/auth.ts"
    tags: [api, auth, login]
    aliases: [auth]
    entrypoints: []
    main_symbols:
      - login
      - refreshTokens
      - logout
    depends_on: [lib-api, stores-session]
    used_by: [app-pages]
    needs_review: false
    concerns: []
    review_reasons: []

  # ─── Token Refresh Single-Flight (2026-06-24-concurrent-refresh-revoke 新增) ──

  lib-token-refresh:
    status: active
    doc: modules/lib-token-refresh.md
    paths:
      - "frontend/src/lib/token-refresh.ts"
    tags: [api, auth, token, single-flight, refresh]
    aliases: [token-refresh]
    entrypoints: []
    main_symbols:
      - ensureFreshAccessToken
      - decodeJwtExp
    depends_on: [stores-session, lib-api]
    used_by: [lib-api, lib-auth, lib-ppm, components-shared]
    needs_review: true
    concerns: [new-module-pending-card]
    review_reasons:
      - 新增模块(2026-06-24-concurrent-refresh-revoke),modules/lib-token-refresh.md 卡片待下次 scan 生成

  # ─── Workspaces Client ─────────────────────────────────────────────

  lib-workspaces:
    status: active
    doc: modules/lib-workspaces.md
    paths:
      - "frontend/src/lib/workspaces.ts"
    tags: [api, workspaces]
    aliases: [workspaces-api]
    entrypoints: []
    main_symbols:
      - scanWorkspace
      - scanGenerate
      - activateWorkspace
      - listWorkspaces
      - createWorkspace
      - rescanWorkspace
      - reparseWorkspace
      - deleteWorkspace
      - getWorkspace
      - getWorkspaceRelations
      - createRelation
      - deleteRelation
      - getTopology
    depends_on: [lib-api]
    used_by: [app-pages, app-workspace-pages, lib-components]
    needs_review: false
    concerns: []
    review_reasons: []

  # ─── Changes Client ────────────────────────────────────────────────

  lib-changes:
    status: active
    doc: modules/lib-changes.md
    paths:
      - "frontend/src/lib/changes.ts"
    tags: [api, changes, sillyspec]
    aliases: [changes-api]
    entrypoints: []
    main_symbols:
      - listChanges
      - getChange
      - getChangeDocuments
      - getChangeDocumentContent
      - reparseChanges
      - getChangeApproval
      - approveChange
      - rejectChange
      - updateChangeProgress
      - createChange
      - executeChange
      - transitionChange
      - submitFeedback
      - checkArchiveGate
      - getAgentStatus
      - triggerDispatch
      - proposalReview
      - planReview
      - humanTest
      - archiveConfirm
    depends_on: [lib-api]
    used_by: [app-workspace-pages]
    needs_review: false
    concerns: []
    review_reasons: []

  # ─── Agent Client ──────────────────────────────────────────────────

  lib-agent:
    status: active
    doc: modules/lib-agent.md
    paths:
      - "frontend/src/lib/agent.ts"
    tags: [api, agent]
    aliases: [agent-api]
    entrypoints: []
    main_symbols:
      - createAgentRun
      - getAgentRun
      - listAgentRuns
      - getAgentRunLogs
      - streamAgentRunLogs
      - killAgentRun
      - submitAgentRunInput
      - listDaemonRuntimes
    depends_on: [lib-api, stores-session]
    used_by: [lib-agent-stream, lib-use-agent-run-stream, lib-spec-workspaces, components-daemon, app-workspace-pages]
    needs_review: false
    concerns: []
    review_reasons: []

  # ─── Agent Stream Client ───────────────────────────────────────────

  lib-agent-stream:
    status: active
    doc: modules/lib-agent-stream.md
    paths:
      - "frontend/src/lib/agent-stream.ts"
    tags: [api, agent, sse, streaming]
    aliases: [agent-stream]
    entrypoints: []
    main_symbols:
      - AgentRunStreamClient
    depends_on: [lib-api, lib-agent, stores-session]
    used_by: [app-workspace-pages]
    needs_review: false
    concerns: []
    review_reasons: []

  # ─── useAgentRunStream Hook（前端流式订阅） ──────────────────────

  lib-use-agent-run-stream:
    status: active
    doc: modules/lib-use-agent-run-stream.md
    paths:
      - "frontend/src/lib/use-agent-run-stream.ts"
    tags: [hook, react, agent, sse, streaming]
    aliases: [use-agent-run-stream]
    entrypoints: []
    main_symbols:
      - useAgentRunStream
      - UseAgentRunStreamOptions
      - UseAgentRunStreamResult
      - AgentRunInputStream
    depends_on: [lib-agent, lib-agent-stream]
    used_by: [components-daemon, app-workspace-pages]
    needs_review: false
    concerns: []
    review_reasons:
      - 新增模块，modules/lib-use-agent-run-stream.md 尚未生成

  # ─── Components Client ─────────────────────────────────────────────

  lib-components:
    status: active
    doc: modules/lib-components.md
    paths:
      - "frontend/src/lib/components.ts"
    tags: [api, components]
    aliases: [components-api]
    entrypoints: []
    main_symbols:
      - listComponents
      - getComponent
      - reparseComponents
      - getTopology
    depends_on: [lib-api, lib-workspaces]
    used_by: [app-workspace-pages]
    needs_review: false
    concerns: []
    review_reasons: []

  # ─── Scan Docs Client ──────────────────────────────────────────────

  lib-scan-docs:
    status: active
    doc: modules/lib-scan-docs.md
    paths:
      - "frontend/src/lib/scan-docs.ts"
    tags: [api, scan-docs]
    aliases: [scan-docs-api]
    entrypoints: []
    main_symbols:
      - listScanDocs
      - getScanDoc
      - reparseScanDocs
    depends_on: [lib-api]
    used_by: [app-workspace-pages]
    needs_review: false
    concerns: []
    review_reasons: []

  # ─── Knowledge Client ──────────────────────────────────────────────

  lib-knowledge:
    status: active
    doc: modules/lib-knowledge.md
    paths:
      - "frontend/src/lib/knowledge.ts"
    tags: [api, knowledge]
    aliases: [knowledge-api]
    entrypoints: []
    main_symbols:
      - listKnowledge
      - getKnowledge
      - listQuicklog
      - getQuicklog
    depends_on: [lib-api]
    used_by: [app-workspace-pages]
    needs_review: false
    concerns: []
    review_reasons: []

  # ─── Tasks Client ──────────────────────────────────────────────────

  lib-tasks:
    status: active
    doc: modules/lib-tasks.md
    paths:
      - "frontend/src/lib/tasks.ts"
    tags: [api, tasks]
    aliases: [tasks-api]
    entrypoints: []
    main_symbols:
      - listTasks
      - getTask
      - getTaskBoard
      - reparseTasks
    depends_on: [lib-api]
    used_by: [app-workspace-pages]
    needs_review: false
    concerns: []
    review_reasons: []

  # ─── Workflow Client ───────────────────────────────────────────────

  lib-workflow:
    status: active
    doc: modules/lib-workflow.md
    paths:
      - "frontend/src/lib/workflow.ts"
    tags: [api, workflow, transitions]
    aliases: [workflow-api]
    entrypoints: []
    main_symbols:
      - transitionChange
      - submitReview
      - listReviews
      - transitionTask
    depends_on: [lib-api]
    used_by: [app-workspace-pages]
    needs_review: false
    concerns: []
    review_reasons: []

  # ─── Releases Client ───────────────────────────────────────────────

  lib-releases:
    status: active
    doc: modules/lib-releases.md
    paths:
      - "frontend/src/lib/releases.ts"
    tags: [api, releases]
    aliases: [releases-api]
    entrypoints: []
    main_symbols:
      - listReleases
      - createRelease
      - approveRelease
      - listApprovals
      - deployRelease
      - promoteRelease
      - rollbackRelease
    depends_on: [lib-api]
    used_by: [app-workspace-pages]
    needs_review: false
    concerns: []
    review_reasons: []

  # ─── Incidents Client ──────────────────────────────────────────────

  lib-incidents:
    status: active
    doc: modules/lib-incidents.md
    paths:
      - "frontend/src/lib/incidents.ts"
    tags: [api, incidents]
    aliases: [incidents-api]
    entrypoints: []
    main_symbols:
      - listIncidents
      - createIncident
      - getIncident
      - updateIncident
      - createPostmortem
      - getPostmortem
    depends_on: [lib-api]
    used_by: [app-workspace-pages]
    needs_review: false
    concerns: []
    review_reasons: []

  # ─── Approvals Client ──────────────────────────────────────────────

  lib-approvals:
    status: active
    doc: modules/lib-approvals.md
    paths:
      - "frontend/src/lib/approvals.ts"
    tags: [api, approvals]
    aliases: [approvals-api]
    entrypoints: []
    main_symbols:
      - listPendingApprovals
      - listApprovalHistory
      - approveRequest
      - rejectRequest
    depends_on: [lib-api]
    used_by: [app-workspace-pages]
    needs_review: false
    concerns: []
    review_reasons: []

  # ─── Audit Client ──────────────────────────────────────────────────

  lib-audit:
    status: active
    doc: modules/lib-audit.md
    paths:
      - "frontend/src/lib/audit.ts"
    tags: [api, audit]
    aliases: [audit-api]
    entrypoints: []
    main_symbols:
      - listAuditLogs
    depends_on: [lib-api]
    used_by: [app-workspace-pages]
    needs_review: false
    concerns: []
    review_reasons: []

  # ─── Health Client ─────────────────────────────────────────────────

  lib-health:
    status: active
    doc: modules/lib-health.md
    paths:
      - "frontend/src/lib/health.ts"
    tags: [api, health]
    aliases: [health-api]
    entrypoints: []
    main_symbols:
      - getHealth
    depends_on: [lib-api]
    used_by: [app-pages]
    needs_review: false
    concerns: []
    review_reasons: []

  # ─── Settings Client ───────────────────────────────────────────────

  lib-settings:
    status: active
    doc: modules/lib-settings.md
    paths:
      - "frontend/src/lib/settings.ts"
    tags: [api, settings, users]
    aliases: [settings-api]
    entrypoints: []
    main_symbols:
      - listSettings
      - updateSettings
      - listUsers
      - createUser
      - updateUser
      - deleteUser
    depends_on: [lib-api]
    used_by: [app-pages]
    needs_review: false
    concerns: []
    review_reasons: []

  # ─── Git Identities Client ─────────────────────────────────────────

  lib-git-identities:
    status: active
    doc: modules/lib-git-identities.md
    paths:
      - "frontend/src/lib/git-identities.ts"
    tags: [api, git, identities]
    aliases: [git-identities-api]
    entrypoints: []
    main_symbols:
      - listGitIdentities
      - createGitIdentity
      - getGitIdentity
      - revokeGitIdentity
      - checkGitAccess
    depends_on: [lib-api]
    used_by: [app-pages]
    needs_review: false
    concerns: []
    review_reasons: []

  # ─── Git Gateway Client ────────────────────────────────────────────

  lib-git-gateway:
    status: active
    doc: modules/lib-git-gateway.md
    paths:
      - "frontend/src/lib/git-gateway.ts"
    tags: [api, git, gateway]
    aliases: [git-gateway-api]
    entrypoints: []
    main_symbols:
      - executeGitOperation
    depends_on: [lib-api]
    used_by: []
    needs_review: false
    concerns: []
    review_reasons: []

  # ─── Tool Gateway Client ───────────────────────────────────────────

  lib-tool-gateway:
    status: active
    doc: modules/lib-tool-gateway.md
    paths:
      - "frontend/src/lib/tool-gateway.ts"
    tags: [api, tools, gateway]
    aliases: [tool-gateway-api]
    entrypoints: []
    main_symbols:
      - executeTool
    depends_on: [lib-api]
    used_by: []
    needs_review: false
    concerns: []
    review_reasons: []

  # ─── Worktree Client ───────────────────────────────────────────────

  lib-worktree:
    status: active
    doc: modules/lib-worktree.md
    paths:
      - "frontend/src/lib/worktree.ts"
    tags: [api, worktree]
    aliases: [worktree-api]
    entrypoints: []
    main_symbols:
      - acquireWorktree
      - listWorktrees
      - getWorktree
      - releaseWorktree
      - extendWorktree
    depends_on: [lib-api]
    used_by: []
    needs_review: false
    concerns: []
    review_reasons: []

  # ─── Runtime Client ────────────────────────────────────────────────

  lib-runtime:
    status: active
    doc: modules/lib-runtime.md
    paths:
      - "frontend/src/lib/runtime.ts"
    tags: [api, runtime]
    aliases: [runtime-api]
    entrypoints: []
    main_symbols:
      - getRuntimeProgress
      - getRuntimeUserInputsRaw
      - getRuntimeArtifacts
      - getRuntimeArtifactContent
    depends_on: [lib-api]
    used_by: [app-workspace-pages]
    needs_review: false
    concerns: []
    review_reasons: []

  # ─── Spec Workspaces Client ────────────────────────────────────────

  lib-spec-workspaces:
    status: active
    doc: modules/lib-spec-workspaces.md
    paths:
      - "frontend/src/lib/spec-workspaces.ts"
    tags: [api, spec-workspaces, sillyspec]
    aliases: [spec-workspaces-api]
    entrypoints: []
    main_symbols:
      - getSpecWorkspace
      - importSpecWorkspace
      - syncSpecWorkspace
      - bootstrapSpecWorkspace
      - updateSpecWorkspace
      - listSpecConflicts
      - resolveSpecConflict
    depends_on: [lib-api, lib-agent]
    used_by: []
    needs_review: false
    concerns: []
    review_reasons: []

  # ─── Archive Client ────────────────────────────────────────────────

  lib-archive:
    status: active
    doc: modules/lib-archive.md
    paths:
      - "frontend/src/lib/archive.ts"
    tags: [api, archive]
    aliases: [archive-api]
    entrypoints: []
    main_symbols:
      - archiveChange
      - distillChange
    depends_on: [lib-api]
    used_by: []
    needs_review: false
    concerns: []
    review_reasons: []

  # ─── Change Writer Client ──────────────────────────────────────────

  lib-change-writer:
    status: active
    doc: modules/lib-change-writer.md
    paths:
      - "frontend/src/lib/change-writer.ts"
    tags: [api, change-writer]
    aliases: [change-writer-api]
    entrypoints: []
    main_symbols:
      - createChange
      - generateDocs
      - batchGenerateDocuments
    depends_on: [lib-api]
    used_by: []
    needs_review: false
    concerns: []
    review_reasons: []

  # ─── Daemon Client ─────────────────────────────────────────────────

  lib-daemon:
    status: active
    doc: modules/lib-daemon.md
    paths:
      - "frontend/src/lib/daemon.ts"
    tags: [api, daemon]
    aliases: [daemon-api]
    entrypoints: []
    main_symbols:
      - listDaemonRuntimes
      - getDaemonRuntime
      - quickChat
      - getQuickChatResult
      - isVersionBelow
    depends_on: [lib-api, app-api-routes]
    used_by: [app-pages, components-daemon]
    needs_review: false
    concerns: []
    review_reasons: []

  # ─── Error Message & Notify Helpers (2026-06-25-frontend-error-handling 新增) ──

  lib-errors:
    status: active
    doc: modules/lib-errors.md
    paths:
      - "frontend/src/lib/errors.ts"
      - "frontend/src/lib/errors.test.ts"
    tags: [errors, notify, antd, i18n, ux]
    aliases: [errors-lib, error-message]
    entrypoints: []
    main_symbols:
      - errMessage
      - useNotify
    depends_on: [lib-api]   # 仅 instanceof ApiError + 读 code/message
    used_by:
      - app-pages            # task-04 runtimes 删除首场景
      - app-workspace-pages  # task-06 D 模式收敛（members）
      - app-admin-pages      # task-06 D 模式收敛（按需）
      - components-shared    # task-06 D 模式收敛（health-card / workspace-scan-dialog）
      - components-admin     # task-06 D 模式收敛（api-key-create-dialog / daemon-dir-browser）
      - stores-kanban        # task-07 store 层 import 全局 errMessage
    needs_review: false
    concerns: []
    review_reasons:
      - 新增模块(2026-06-25-frontend-error-handling),展示策略规范见 modules/lib-errors.md 注意事项区

  # ─── Admin Client（用户/角色/组织管理 API） ──────────────────────

  lib-admin:
    status: active
    doc: modules/lib-admin.md
    paths:
      - "frontend/src/lib/admin.ts"
    tags: [api, admin, users, roles, organizations]
    aliases: [admin-api]
    entrypoints: []
    main_symbols:
      - listUsers
      - getUser
      - createUser
      - updateUser
      - deleteUser
      - listUserSessions
      - revokeUserSession
      - revokeAllUserSessions
      - listUserAudit
      - listUserWorkspaces
      - resetUserPassword
      - disableUserLogin
      - enableUserLogin
      - listOrganizations
      - getOrganization
      - createOrganization
      - updateOrganization
      - disableOrganization
      - enableOrganization
      - deleteOrganization
      - listRoles
      - getRole
      - createRole
      - updateRole
      - disableRole
      - enableRole
      - deleteRole
      - listRoleUsers
    depends_on: [lib-api]
    used_by: [app-admin-pages, components-admin]
    needs_review: false
    concerns: []
    review_reasons:
      - 新增模块，modules/lib-admin.md 尚未生成

  # ─── API Keys Client ───────────────────────────────────────────────

  lib-api-keys:
    status: active
    doc: modules/lib-api-keys.md
    paths:
      - "frontend/src/lib/api-keys.ts"
    tags: [api, api-keys]
    aliases: [api-keys-api]
    entrypoints: []
    main_symbols:
      - listApiKeys
      - createApiKey
      - revokeApiKey
      - getLatestActiveApiKey
    depends_on: [lib-api]
    used_by: [app-pages, components-admin]
    needs_review: false
    concerns: []
    review_reasons:
      - 新增模块，modules/lib-api-keys.md 尚未生成

  # ─── Workspace Members Client ──────────────────────────────────────

  lib-workspace-members:
    status: active
    doc: modules/lib-workspace-members.md
    paths:
      - "frontend/src/lib/workspace-members.ts"
    tags: [api, workspace-members]
    aliases: [workspace-members-api]
    entrypoints: []
    main_symbols:
      - listMembers
      - searchUsersForInvite
      - addMember
      - updateMemberRole
      - removeMember
      - transferOwnership
    depends_on: [lib-api]
    used_by: [app-workspace-pages, components-admin]
    needs_review: false
    concerns: []
    review_reasons:
      - 新增模块，modules/lib-workspace-members.md 尚未生成

  # ─── Permission Helpers（菜单/权限判定） ──────────────────────────

  lib-permission:
    status: active
    doc: modules/lib-permission.md
    paths:
      - "frontend/src/lib/permission.ts"
    tags: [permission, rbac, menu]
    aliases: [permission-helpers]
    entrypoints: []
    main_symbols:
      - hasAdminPermission
      - hasAnyPermission
      - canSeeMenu
      - visibleMenusBySection
    depends_on: [stores-session]
    used_by: [app-layouts, components-permissions, components-shared]
    needs_review: false
    concerns: []
    review_reasons:
      - 新增模块，modules/lib-permission.md 尚未生成

  # ─── Menu Permissions（菜单权限定义） ─────────────────────────────

  lib-menu-permissions:
    status: active
    doc: modules/lib-menu-permissions.md
    paths:
      - "frontend/src/lib/menu-permissions.ts"
    tags: [permission, menu, rbac]
    aliases: [menu-permissions]
    entrypoints: []
    main_symbols:
      - MENU_PERMISSION_GROUPS
      - MENU_SECTION_ORDER
      - MENU_SECTION_LABEL
    depends_on: []
    used_by: [app-layouts, components-permissions, lib-permission]
    needs_review: false
    concerns: []
    review_reasons:
      - 新增模块，modules/lib-menu-permissions.md 尚未生成

  # ─── PPM Client（项目管理 API 客户端集合） ───────────────────────

  lib-ppm:
    status: active
    doc: modules/lib-ppm.md
    paths:
      - "frontend/src/lib/ppm/*.ts"
    tags: [api, ppm, project-management, kanban]
    aliases: [ppm-api, ppm-lib]
    entrypoints: []
    main_symbols:
      - listProjects
      - listPlans
      - listProblems
      - listTasks
      - fetchKanban
      - downloadExcel
      - statusLabel
      - KanbanGrouping
    depends_on: [lib-api]
    used_by: [app-ppm-pages, components-ppm, stores-kanban]
    needs_review: false
    concerns: []
    review_reasons:
      - 新增模块（含 13 个子文件），modules/lib-ppm.md 尚未生成

  # ─── Path / Format 辅助工具 ───────────────────────────────────────

  lib-utils:
    status: active
    doc: modules/lib-utils.md
    paths:
      - "frontend/src/lib/utils.ts"
      - "frontend/src/lib/client-path.ts"
      - "frontend/src/lib/workspace-path.ts"
      - "frontend/src/lib/format-token.ts"
      - "frontend/src/lib/status-labels.ts"
    tags: [utils, tailwind, clsx, format, labels]
    aliases: [utils]
    entrypoints: []
    main_symbols:
      - cn
      - normalizeClientPath
      - joinClientPath
      - parentClientPath
      - isDaemonClientWorkspace
      - workspacePathSourceLabel
      - formatDaemonRuntimeSummary
      - daemonRuntimeStatusVariant
      - formatTokenCount
      - STATUS_LABELS
      - DAEMON_RUNTIME_STATUS_LABELS
      - AUDIT_RESOURCE_TYPE_LABELS
      - APPROVAL_STATUS_LABELS
      - labelOf
    depends_on: []
    used_by: [components-ui, app-workspace-pages, components-shared, app-pages, app-ppm-pages]
    needs_review: false
    concerns: []
    review_reasons: []

  # ─── Session Store ─────────────────────────────────────────────────

  stores-session:
    status: active
    doc: modules/stores-session.md
    paths:
      - "frontend/src/stores/session.ts"
    tags: [store, zustand, auth, session, persist]
    aliases: [session-store, 状态管理]
    entrypoints: []
    main_symbols:
      - useSession
      - SessionUser
      - SessionTokens
    depends_on: []
    used_by:
      - lib-api
      - lib-auth
      - lib-agent
      - lib-agent-stream
      - lib-permission
      - app-layouts
      - components-shared
      - components-daemon
      - app-pages
      - app-admin-pages
      - app-ppm-pages
      - app-workspace-pages
    needs_review: false
    concerns: []
    review_reasons: []

  # ─── Kanban Store（看板筛选状态） ────────────────────────────────

  stores-kanban:
    status: active
    doc: modules/stores-kanban.md
    paths:
      - "frontend/src/stores/kanban.ts"
    tags: [store, zustand, kanban, ppm]
    aliases: [kanban-store]
    entrypoints: []
    main_symbols:
      - useKanbanStore
      - KanbanFilters
    depends_on: [lib-ppm]
    used_by: [app-ppm-pages]
    needs_review: false
    concerns: []
    review_reasons:
      - 新增模块，modules/stores-kanban.md 尚未生成

  # ─── Test Infrastructure ───────────────────────────────────────────

  test-utils:
    status: active
    doc: modules/test-utils.md
    paths:
      - "frontend/src/test/setup.ts"
      - "frontend/src/lib/__tests__/*.test.ts"
      - "frontend/src/components/__tests__/*.test.ts"
      - "frontend/src/components/daemon/__tests__/*"
      - "frontend/src/components/agent-log/__tests__/*"
      - "frontend/src/lib/ppm/__tests__/*"
    tags: [testing, vitest]
    aliases: [tests, 测试, test]
    entrypoints: []
    main_symbols: []
    depends_on: [lib-api, lib-agent, lib-spec-workspaces, lib-admin, lib-permission, lib-menu-permissions, lib-client-path, lib-workspace-path, lib-format-token, lib-ppm, lib-use-agent-run-stream, lib-daemon]
    used_by: []
    needs_review: false
    concerns:
      - 测试集中在 lib 层，页面/组件集成测试覆盖偏低
    review_reasons: []

  # ─── Styles ────────────────────────────────────────────────────────

  styles:
    status: active
    doc: modules/styles.md
    paths:
      - "frontend/src/app/globals.css"
    tags: [css, tailwind, theme, variables]
    aliases: [styles, 样式]
    entrypoints: []
    main_symbols: []
    depends_on: []
    used_by: [app-layouts]
    needs_review: false
    concerns: []
    review_reasons: []

  # ─── Build Config ──────────────────────────────────────────────────

  build:
    status: active
    doc: modules/build.md
    paths:
      - "frontend/package.json"
      - "frontend/next.config.mjs"
      - "frontend/tailwind.config.ts"
      - "frontend/tsconfig.json"
      - "frontend/vitest.config.ts"
      - "frontend/postcss.config.mjs"
      - "frontend/Dockerfile"
      - "frontend/components.json"
    tags: [build, config]
    aliases: [project-config, build]
    entrypoints: []
    main_symbols: []
    depends_on: []
    used_by: []
    needs_review: false
    concerns: []
    review_reasons: []
