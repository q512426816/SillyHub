# D-009@v1：只用系统 claude.CMD（driver pathToClaudeCodeExecutable），不带 SDK 内置
# 平台二进制（@anthropic-ai/claude-agent-sdk-win32-x64，224MB）。
#
# 排除方式：package.json `pnpm.overrides` 把平台二进制子包别名到主 JS 包
#（npm:@anthropic-ai/claude-agent-sdk@0.3.181）。主包只含 sdk.mjs / sdk.d.ts，
# 不含 claude.exe，pnpm 装它替代 214MB 的 exe 子包。
#
# 不用 `optional=false`：pnpm 会连带跳过 vitest 的 @rollup/rollup-win32-x64-msvc 等
# 其他正当 optional 依赖，导致 vitest 启动即崩溃（MODULE_NOT_FOUND）。精确 overrides
# 只命中目标子包，保留所有其他 optional 依赖，现有测试零回归。
