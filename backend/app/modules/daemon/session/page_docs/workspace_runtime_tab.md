## 功能定位
工作区运行时 tab：本工作区绑定的机器与引擎配置——会话默认派发到哪台机器、用哪个默认智能体/模型。

## 页面结构与操作
1. 查看/管理本工作区绑定的机器（成员-机器绑定关系）。
2. 配置默认智能体与默认模型（新建会话的下 Defaults）。

## 典型工作流
项目机器约定：本项目固定走某台构建机 → 此处绑定并设默认 → 会话创建免选。

## 常见问题
- 会话派到别的机器：检查默认绑定与在线状态；显式选择优先于默认。
