## 功能定位
管理平台自身的 API 密钥：供外部程序（CLI、脚本、CI）以编程方式调用本平台开放 API 时做身份认证。

## 页面结构与操作
1. 列表查看已创建密钥（名称、权限范围、创建时间）。
2. 创建密钥：命名并按需限定权限；创建时完整展示一次，请立即妥善保存。
3. 可随时吊销失效。

## 典型工作流
给 CI 脚本接入：创建密钥 → 复制保存 → 脚本请求头携带调用平台 API。

## 常见问题
- 密钥只显示一次：创建弹窗关闭后无法再查看明文，丢失请吊销重建。
