## 功能定位
工作区级 MCP 服务器配置：本工作区智能体可连接的外部工具服务（检索/数据库/三方 API），作用域限定于本工作区。

## 页面结构与操作
1. 列表/新增/编辑 MCP 服务器（传输方式、命令或 URL、环境变量），测试连接。
2. 工作区档案引用后，会话即可调用其工具。

## 典型工作流
项目数据库助手：加一个数据库 MCP → 挂到项目档案 → 会话里让 AI 查数据。

## 常见问题
- 工具不出现：服务器在线 + 档案已引用 + 会话在引用后创建/切换。
