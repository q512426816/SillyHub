# Public — exposed to the browser (must be NEXT_PUBLIC_ prefixed).
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Optional: build-time commit metadata. Injected by CI / docker build args.
NEXT_PUBLIC_COMMIT_SHA=
